# Stripe Settings (Cloud mode only)
# ※ローカルで分析するだけなら不要ですが、コード上はキーの存在チェックがあります
STRIPE_SECRET_KEY=sk_test_dummy
STRIPE_PAYMENT_LINK=https://example.com/payment

# Database (Optional)
# ※設定しない場合、自動的にローカルのSQLite (note_data/note_dashboard.db) が使用されます
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Admin Settings (Optional)
ADMIN_EMAIL=admin@example.com

# Auto Login Credentials (Optional)
# ※毎回入力するのが面倒な場合、ここに設定しておくとログイン画面に自動入力されます
NOTE_EMAIL=
NOTE_PASSWORD=
